# 统一 DPP 平台阶段 6：测试与发布验收记录

更新日期：2026-07-25

阶段状态：已完成；本地自动化、权限负向测试、响应式浏览器检查、Preview 人工验收、Production 部署和上线后冒烟测试均已通过

Preview 部署：

- 状态：`READY`
- 目标：`preview`
- 部署 ID：`dpl_GaS35XSCj3ACXARyzinkGrhis934`
- 地址：<https://greanlean-dpp-v0-26qd4xv0i-songweinie-1175s-projects.vercel.app>
- Vercel Deployment Protection：已启用
- 登录成功后使用完整页面跳转重新解析访问身份和 DPP 可见范围。

Production 部署：

- 状态：`READY`
- 目标：`production`
- 部署 ID：`dpl_FRBrfbK2g6ksZP3m1kk2mPetZXzv`
- 部署地址：<https://greanlean-dpp-v0-cpvgs038x-songweinie-1175s-projects.vercel.app>
- 正式域名：<https://www.greanlean.com>

## 1. 自动化结果

- Repository hygiene：通过；
- TypeScript：通过；
- 业务、单元和接口测试：56 / 56 通过；
- 数据库迁移测试：11 / 11 通过；
- Next.js Production Build：通过；
- `git diff --check`：通过。

## 2. 数据库验收

- 阶段 5 数据库结构检查：6 / 6 通过；
- LMT 电池：270 条初始化运行指标、2 条生命周期事件；
- 固定式工业储能电池：270 条初始化运行指标、2 条生命周期事件；
- 初始化数据均标记为 `INITIAL_DATASET` 和 `UNVERIFIED`；
- 接入凭据只保存哈希；
- 接入请求、运行指标和生命周期事件采用只追加设计。

## 3. 四个案例回归

已检查以下中文和英文页面：

1. LMT 电动自行车电池；
2. 固定式工业储能电池；
3. 纺织面料；
4. 消费电子无线耳机。

本地桌面浏览器结果：

- 页面均正常加载，无 404 和应用错误；
- 四个案例使用同一 DPP 页面骨架；
- 电池案例包含“运行状态与电池健康”模块；
- 纺织和消费电子案例不会误显示电池动态模块；
- 中文页面没有未翻译的说明性英文；
- 英文页面没有中文数据回退；
- 页面无横向溢出；
- 折叠按钮状态和文案一致；
- 浏览器端 Supabase Auth 客户端使用单例，无重复实例警告。

响应式浏览器结果：

- 官网与四个 DPP 页面在 390 px、768 px 和 1440 px 均无横向溢出；
- 结合此前 1280 px 检查，四个目标视口均已覆盖；
- 10 个手机和平板页面组合均无破图和应用错误；
- 390 px LMT 电池护照已完成画面抽查，页头、标题、图片和身份字段均正常换行；
- 四个 DPP 页面图片均具备替代文本，交互元素均具备可访问名称；
- 官网 Hero 图片为空替代文本，因为它属于装饰性背景；
- 官网隐藏蜜罐输入框不可聚焦，不属于用户交互控件；
- 浏览器控制台未发现应用错误。

保留的英文内容仅限品牌、型号、URL、标准和行业缩写，例如 `GTIN`、`SGTIN`、`NMC`、`BMS`、`RoHS`、`REACH` 和 `WEEE`。

## 4. 权限和接口负向测试

| 场景 | 预期 | 实际 |
| --- | --- | --- |
| 读取公开电池护照 | `200` | 通过 |
| 未登录读取访问上下文 | `401` | 通过 |
| 未登录读取单体动态指标 | `401` | 通过 |
| 无设备凭据写入动态指标 | `401` | 通过 |
| 未登录创建设备凭据 | `401` | 通过 |
| 公开 JSON 搜索动态指标字段 | 0 个命中 | 通过 |
| 通过 URL 请求专业或监管视图 | 不提升权限 | 通过 |

无设备凭据写入接口原先会在缺少本地服务端密钥时先返回 `500`，现已调整为先校验设备凭据并返回 `401 INVALID_INTEGRATION_CREDENTIAL`。

## 5. Preview 验收结果

- Vercel Preview 环境已确认配置 `SUPABASE_SERVICE_ROLE_KEY`；
- Vercel 云端 Production Build 已通过；
- 首页与四个 DPP 页面均返回 `200` 且包含预期产品内容；
- 公开电池 API 返回 `200`，未发现单体动态指标字段；
- 未登录读取单体动态指标返回 `401 AUTH_REQUIRED`；
- 平台管理员账号已登录并人工确认电池授权页面正常；
- 未登录和无授权访问由权限契约测试、公开页面检查和受限 API `401/403` 测试覆盖；
- 390 px、768 px、1280 px 和 1440 px 视口检查已通过；
- 交互控件可访问名称和图片替代文本检查已通过；
- Preview 构建和请求日志未发现服务端异常；
- 人工确认后才允许发布 Production。

## 6. 发布门禁

项目负责人已确认 Production 发布。云端构建成功，正式域名已切换到本次部署。

上线后检查结果：

- 官网、登录页和四个主要 DPP 页面均返回 `200`；
- 页面包含对应的中文产品名称和首页主标题；
- 公开 LMT 电池 API 返回 `200`，未发现受限运行遥测字段；
- 未登录读取单体运行指标返回 `401 AUTH_REQUIRED`；
- 正式域名 HTTPS 访问正常。
