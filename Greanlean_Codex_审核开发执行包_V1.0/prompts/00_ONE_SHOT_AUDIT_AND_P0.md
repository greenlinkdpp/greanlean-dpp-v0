请读取根目录 `AGENTS.md`、`README_CODEX.md` 和 `CODEX_MASTER_TASK.md`，按主任务完整执行。

要求：
1. Phase A只审核，不修改应用代码；
2. 审核完成后，如未触发AGENTS.md停止条件，直接进入技术设计和P0开发，不等待我确认；
3. 严格复用现有技术栈和目录，不从零重写；
4. 所有审核、设计、测试和发布文档写入`docs/generated/`；
5. 每个P0 Epic必须有自动化测试、迁移影响和需求追踪；
6. 不得虚构法规、真实客户数据或Registry成功；
7. 最终给出实际完成范围、命令、测试结果、部署/回滚和未决事项。

现在从Phase A开始。
