"use client";

import Link from "next/link";
import { FormEvent, useState } from "react";
import { PublicHeader } from "@/components/PublicHeader";
import { useLanguage } from "@/components/LanguageProvider";
import { assessBatteryApplicability, type ApplicabilityResult } from "@/lib/p0/applicability";

export function PublicApplicabilityAssessment() {
  const { locale } = useLanguage();
  const zh = locale === "zh";
  const [result, setResult] = useState<ApplicabilityResult | null>(null);
  const [form, setForm] = useState({ batteryCategory: "LMT", intendedUse: "electric bicycle", ratedEnergyKwh: "0.72", euMarketStatus: "PLANNED", placingOperatorRole: "MANUFACTURER", disclaimerAcknowledged: false });

  function submit(event: FormEvent) {
    event.preventDefault();
    setResult(assessBatteryApplicability({ ...form, ratedEnergyKwh: form.ratedEnergyKwh ? Number(form.ratedEnergyKwh) : null, euMarketStatus: form.euMarketStatus as any, disclaimerAcknowledged: form.disclaimerAcknowledged, availableEvidence: [] }));
  }

  return <><PublicHeader /><main className="min-h-screen bg-slate-50"><section className="border-b border-slate-200 bg-slate-950 py-14 text-white"><div className="mx-auto max-w-6xl px-6"><p className="text-sm font-black uppercase text-emerald-300">{zh ? "电池护照准备" : "Battery passport readiness"}</p><h1 className="mt-4 max-w-4xl text-4xl font-black leading-tight md:text-5xl">{zh ? "用五项产品事实完成适用性初评" : "Assess preliminary scope from five product facts"}</h1><p className="mt-5 max-w-3xl text-lg font-semibold leading-8 text-slate-300">{zh ? "结果会显示规则版本、待确认项和下一步资料，不构成法律认证。" : "The result includes the rule version, open questions and next inputs. It is not legal certification."}</p></div></section><section className="mx-auto grid max-w-6xl gap-8 px-6 py-12 lg:grid-cols-[420px_minmax(0,1fr)]"><form onSubmit={submit} className="h-fit border border-slate-200 bg-white p-6"><div className="space-y-5"><label className="block text-sm font-bold text-slate-700">{zh ? "电池类别" : "Battery category"}<select className="input mt-2 w-full" value={form.batteryCategory} onChange={(event) => setForm({ ...form, batteryCategory: event.target.value })}><option value="LMT">LMT</option><option value="EV">EV</option><option value="INDUSTRIAL">{zh ? "工业电池" : "Industrial"}</option><option value="PORTABLE">{zh ? "便携式" : "Portable"}</option><option value="SLI">SLI</option></select></label><label className="block text-sm font-bold text-slate-700">{zh ? "预期用途" : "Intended use"}<input className="input mt-2 w-full" required value={form.intendedUse} onChange={(event) => setForm({ ...form, intendedUse: event.target.value })} /></label><label className="block text-sm font-bold text-slate-700">{zh ? "额定能量 (kWh)" : "Rated energy (kWh)"}<input type="number" min="0" step="0.001" className="input mt-2 w-full" value={form.ratedEnergyKwh} onChange={(event) => setForm({ ...form, ratedEnergyKwh: event.target.value })} /></label><label className="block text-sm font-bold text-slate-700">{zh ? "欧盟市场" : "EU market"}<select className="input mt-2 w-full" value={form.euMarketStatus} onChange={(event) => setForm({ ...form, euMarketStatus: event.target.value })}><option value="YES">{zh ? "已投放" : "Yes"}</option><option value="PLANNED">{zh ? "计划投放" : "Planned"}</option><option value="NO">{zh ? "不投放" : "No"}</option><option value="UNKNOWN">{zh ? "待确认" : "Unknown"}</option></select></label><label className="block text-sm font-bold text-slate-700">{zh ? "投放市场主体" : "Placing operator"}<select className="input mt-2 w-full" value={form.placingOperatorRole} onChange={(event) => setForm({ ...form, placingOperatorRole: event.target.value })}><option>MANUFACTURER</option><option>IMPORTER</option><option>DISTRIBUTOR</option><option>AUTHORISED_REPRESENTATIVE</option></select></label><label className="flex items-start gap-3 text-sm font-semibold leading-6 text-slate-600"><input type="checkbox" className="mt-1" checked={form.disclaimerAcknowledged} onChange={(event) => setForm({ ...form, disclaimerAcknowledged: event.target.checked })} /><span>{zh ? "我理解本结果是初步判断，需要结合最终法规和产品事实确认。" : "I understand this is preliminary and requires confirmation against final requirements and product facts."}</span></label></div><button disabled={!form.disclaimerAcknowledged} className="btn-primary mt-6 w-full disabled:opacity-40">{zh ? "生成初评结果" : "Generate preliminary result"}</button></form><div className="border-y border-slate-200 bg-white p-6">{result ? <><p className="text-xs font-black uppercase text-emerald-700">{result.ruleVersion}</p><h2 className="mt-3 text-2xl font-black text-slate-950">{result.result}</h2><p className="mt-3 text-sm font-semibold leading-7 text-slate-600">{result.reason}</p><div className="mt-7 border-t border-slate-200 pt-6"><h3 className="font-black text-slate-950">{zh ? "下一步缺口" : "Next gaps"}</h3><ol className="mt-4 divide-y divide-slate-200 border-y border-slate-200">{result.tasks.map((task, index) => <li key={`${task.title}-${index}`} className="grid gap-2 py-4 sm:grid-cols-[36px_minmax(0,1fr)_80px]"><span className="text-xs font-black text-emerald-700">{String(index + 1).padStart(2, "0")}</span><div><p className="font-black text-slate-950">{task.title}</p><p className="mt-1 text-sm font-semibold text-slate-500">{task.description}</p></div><span className="text-xs font-black text-red-700">{task.priority}</span></li>)}</ol></div><Link href={`/login?lang=${locale}`} className="btn-primary mt-6">{zh ? "进入后台创建试点" : "Create a pilot project"}</Link></> : <div className="grid min-h-96 place-items-center text-center"><div><p className="text-sm font-black uppercase text-slate-400">{zh ? "尚未评估" : "Not assessed"}</p><p className="mt-3 max-w-md text-sm font-semibold leading-7 text-slate-500">{zh ? "填写左侧产品事实后，系统会给出可追踪的初步结果、待确认问题和资料任务。" : "Provide the product facts to generate a traceable preliminary result, open questions and evidence tasks."}</p></div></div>}</div></section></main></>;
}
